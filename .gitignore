# dependencies
node_modules/
.pnp
.pnp.js

# build
.next/
out/
dist/
build/
next-env.d.ts
*.tsbuildinfo

# env / secrets — NEVER commit real credentials
.env
.env.local
.env.production
.env.staging
*.pem
*.key

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# test / coverage
coverage/
*.lcov

# misc
*.log
tmp/

# local PostgreSQL data (audit environment)
pgdata/
pg.log
# audit DB maintenance helpers (local env only)
scripts/_db_maint.mjs
scripts/_db_reset.mjs

# P3 release governance — generated identity (non-secret, build-time)
src/lib/release/generated-identity.json
public/release-identity.json
.next/RELEASE_IDENTITY.json
