# Ujenzi lifecycle integration

**IMPLEMENTED:** work-package progress (unit-checked, earned-value **PARTIAL**), dependency cycle refusal, ITP FAIL → NCR (`NOT_CERTIFIED`), handover readiness gate (open NCR / uncertified commissioning), asset only from **APPROVED** commissioning, BOQ↔WP unit check, Twin kinds for WP/ITP/commissioning, Noelia `ujenzi.handover.check` (observe only, `humanReviewRequired`).

**NOT:** BIM viewer, government APIs, auto-certification, CAP_POSTING, earned-value as a complete system.
