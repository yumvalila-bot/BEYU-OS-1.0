# BEYU OS 1.0 BASELINE (measured)

Date: 2026-09-05
Commit: `6c2ec2663c4f704fd6ca4054d0f9ddedb8fb3878`
Branch: `arena/01a072db-beyu-os-1-0` (baseline commit equals working head)
Working tree: clean at capture (coverage artifacts generated by test runs were reverted; only migration evidence added afterward)
Reproduction helper: `node scripts/migration/capture-reality.mjs`

---

## 1. Environment

| Component | Value |
|---|---|
| OS | Debian 12 (bookworm) |
| Node | v22.22.3 |
| npm | 10.9.8 |
| pnpm | 9.12.3 (installed for source baseline; root is npm) |
| PostgreSQL | **NOT AVAILABLE** (no `psql`, no server; `deb.debian.org`/`binaries.prisma.sh` unreachable) |
| Flutter | **NOT AVAILABLE** |
| Package manager | npm (root), npm (sectors/health/backend), npm (sectors/health) |
| Framework (root) | Next.js 16.3.3, React 19.2.6, Drizzle ORM 0.45.2, Vitest 3.2.7 |

---

## 2. Root BEYU OS baseline

### 2.1 Install

```bash
npm install --no-audit --no-fund
```
Result: **OK** — 442 packages added, 0 fatal errors.

### 2.2 Typecheck

```bash
npm run typecheck   # tsc --noEmit
```
Result: **PASS** (0 errors).

### 2.3 Lint

```bash
npm run lint   # eslint .
```
Result: **PASS** (0 errors, 0 warnings).

### 2.4 Build

```bash
npm run build   # next build
```
Result: **PASS**. Full route inventory emitted (api routes `/api/v1/*`, app routes `/os/*`). No `DATABASE_URL` needed at build time — consistent with the repository's lazy-Pool architecture.

### 2.5 Test (full root suite)

```bash
npm test   # vitest run
```

| Metric | Value |
|---|---|
| Test files | 111 (41 passed, 58 failed, 12 skipped) |
| Tests | 2375 total |
| Passed | **1109** |
| Failed | **450** |
| Skipped | **816** |
| Duration | 61.58s |
| Exit | 1 |

Root cause of failures: `Error: DATABASE_URL is required`. The root suite contains governed-mutation suites that connect to real PostgreSQL (ledger integrity, CAP_POSTING, RLS isolation, audit concurrency/atomicity, activation-gate, policy-provenance, Noelia DB security, runtime-privilege audit, tenant isolation, etc.). Without a PostgreSQL service those suites cannot execute.

### 2.6 Root subsystem spot-runs (no DB)

```bash
npx vitest run tests/finance tests/family
```
Result: 32 files → 22 passed, 9 failed, 1 skipped; **643 passed / 146 failed / 35 skipped** (824 total). The failed cases are DB-backed ledger-integrity / governed-mutation tests.

---

## 3. Health OS backend baseline (`sectors/health/backend`)

### 3.1 Install

```bash
cd sectors/health/backend && npm install --no-audit --no-fund
```
Result: **OK** — 927 packages added.

### 3.2 Build

```bash
npm run build   # nest build
```
Result: **PASS** (rimraf + nest build; 0 errors).

### 3.3 Test

```bash
npm test   # jest (node --experimental-vm-modules)
```

| Metric | Value |
|---|---|
| Test suites | 90 (88 passed, 2 skipped) |
| Tests | 503 (`488 passed`, `15 skipped`) |
| Failures | **0** |
| Duration | 206.0s |
| Exit | 0 |

Domain evidence: suites exercise `ophthalmology.service`, `pharmacy.service`, `laboratory`/`radiology` (via service specs), `dialysis.service`, `ai-governance.fail-closed`, `rls-isolation`, `rls-adversarial`, `migration-roundtrip`, `clinical-safety`, `consent-guard.adversarial`, `csrf-adversarial`, `rate-limit-adversarial`, `transaction-interceptor`, `migration-consistency`, plus test/e2e `app-boot`, `concurrency`, `supabase-retirement`, `clinical-workflow`.

Note: the Health backend suite runs largely against `@electric-sql/pglite` (in-process WASM PostgreSQL). A small set of tests requires the real-PostgreSQL branch and is exercised in CI (see `.github/workflows/ci.yml`), not in this sandbox.

### 3.4 Known baseline debt (pre-existing, not introduced)

- CI explicitly runs `eslint` on `src` without `--fix` and expects a **non-zero** exit today due to documented formatting/unused-var lint debt. Not a security-rule violation; tracked separately.
- Apollo Server v4 is EOL (Jan 2026); `uuid@9`, `eslint@8` are in maintenance.

---

## 4. Health OS frontend baseline (`sectors/health`)

```bash
cd sectors/health
npm run typecheck   # PASS
npm test            # 3 files, 14 tests passed / 0 failed
```

---

## 5. Flutter baseline (`mobile/flutter`)

**BLOCKED** — Flutter SDK not available in this sandbox. Verified by static inspection only:

- Real Dart source present: `lib/main.dart`, `services/api_client.dart`, `services/secure_storage_service.dart`, `providers/auth_provider.dart`, `providers/router_provider.dart`, `screens/*` (splash, login, MFA, launcher, OS shell, access denied).
- `pubspec.yaml` lists `dio`, `flutter_secure_storage`, `provider`, `json_annotation`, `uni_links`, `logger`, `intl`.

---

## 6. Database / migration baseline

| Area | Count / state |
|---|---|
| Root Drizzle migrations | 0000–0022 (23 named SQL migrations + snapshots) |
| Health backend SQL migrations | 24 `.up.sql` (001–024) plus down files |
| RLS model | present in both root and Health migrations (`row_level_security`, `beyu_identity`, `beyu_health` etc.) |
| Runtime vs test role separation | documented in `.env.example`; requires real PostgreSQL to verify |

Not executed in sandbox: applying migrations to real PostgreSQL, seeding, RLS adversarial checks, ledger immutability, CAP_POSTING, audit chain concurrency. These are recorded as **BLOCKED - requires PostgreSQL** in the migration register.

---

## 7. Baseline verdict

- **Non-DB verification**: PASS (typecheck, lint, build, 1109 root tests, 488 Health-backend tests, 14 Health-frontend tests, static Flutter inspection).
- **DB-backed verification**: **BLOCKED** in this environment. The destination baseline is NOT fully reproduced here because the PostgreSQL service is absent.
- Any future migration change touching finance/ledger/RLS/audit/CAP_POSTING **must** re-run in a PostgreSQL-provisioned environment before it can be certified.
